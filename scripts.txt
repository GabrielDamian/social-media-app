CREATE TABLE USERS (
    USER_ID INT(4) PRIMARY KEY ,
    USERNAME VARCHAR(30),
    PASSWORD VARCHAR(30)
);

CREATE TABLE DESCRIPTION (
    ID INT(4),
    DATA VARCHAR(30),
	
);
ALTER TABLE DESCRIPTION ADD CONSTRAINT DESC_FK_USERS FOREIGN KEY (ID) REFERENCES USERS(USER_ID);



